# Copy to .env.local for local dev. On Cloudflare Pages, set these as env vars.

# Required for the AYI_all chat interface (invoke by typing "ayi" or "hello world").
# Get a key: https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=
